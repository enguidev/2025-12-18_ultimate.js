//--------------------------------------------------------------------------------------
// 📋 CHEATSHEET COMPLETO DE MÉTODOS DE ARRAYS
//--------------------------------------------------------------------------------------

/*
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        MÉTODOS QUE MODIFICAN EL ARRAY ORIGINAL                      │
├───────────────┬─────────────┬───────────────────────────────────────────────────────┤
│ Método        │ Devuelve    │ Descripción                                           │
├───────────────┼─────────────┼───────────────────────────────────────────────────────┤
│ push()        │ length      │ Añade elemento(s) al final                            │
│ pop()         │ elemento    │ Elimina y devuelve el último elemento                 │
│ unshift()     │ length      │ Añade elemento(s) al inicio                           │
│ shift()       │ elemento    │ Elimina y devuelve el primer elemento                 │
│ splice()      │ eliminados  │ Añade/elimina/reemplaza elementos en cualquier pos.   │
│ sort()        │ array       │ Ordena el array (por defecto como strings)            │
│ reverse()     │ array       │ Invierte el orden de los elementos                    │
│ fill()        │ array       │ Rellena con un valor dado                             │
│ copyWithin()  │ array       │ Copia parte del array dentro del mismo                │
└───────────────┴─────────────┴───────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                     MÉTODOS QUE NO MODIFICAN EL ARRAY ORIGINAL                      │
├───────────────┬─────────────┬───────────────────────────────────────────────────────┤
│ Método        │ Devuelve    │ Descripción                                           │
├───────────────┼─────────────┼───────────────────────────────────────────────────────┤
│ concat()      │ nuevo array │ Une arrays                                            │
│ slice()       │ nuevo array │ Extrae una porción del array                          │
│ map()         │ nuevo array │ Transforma cada elemento                              │
│ filter()      │ nuevo array │ Filtra elementos que cumplen condición                │
│ flat()        │ nuevo array │ Aplana arrays anidados                                │
│ flatMap()     │ nuevo array │ map() + flat(1)                                       │
│ toSorted()    │ nuevo array │ Ordena sin mutar (ES2023)                             │
│ toReversed()  │ nuevo array │ Invierte sin mutar (ES2023)                           │
│ toSpliced()   │ nuevo array │ splice() sin mutar (ES2023)                           │
│ with()        │ nuevo array │ Reemplaza un elemento sin mutar (ES2023)              │
└───────────────┴─────────────┴───────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          MÉTODOS DE BÚSQUEDA Y ACCESO                               │
├───────────────┬─────────────┬───────────────────────────────────────────────────────┤
│ Método        │ Devuelve    │ Descripción                                           │
├───────────────┼─────────────┼───────────────────────────────────────────────────────┤
│ includes()    │ boolean     │ Verifica si contiene un valor                         │
│ indexOf()     │ índice/-1   │ Índice del primer elemento coincidente                │
│ lastIndexOf() │ índice/-1   │ Índice del último elemento coincidente                │
│ find()        │ elemento    │ Primer elemento que cumple condición                  │
│ findIndex()   │ índice/-1   │ Índice del primer elemento que cumple condición       │
│ findLast()    │ elemento    │ Último elemento que cumple condición (ES2023)         │
│ findLastIndex │ índice/-1   │ Índice del último elemento (ES2023)                   │
│ at()          │ elemento    │ Accede por índice (admite negativos) (ES2022)         │
└───────────────┴─────────────┴───────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              MÉTODOS DE ITERACIÓN                                   │
├───────────────┬─────────────┬───────────────────────────────────────────────────────┤
│ Método        │ Devuelve    │ Descripción                                           │
├───────────────┼─────────────┼───────────────────────────────────────────────────────┤
│ forEach()     │ undefined   │ Ejecuta función por cada elemento                     │
│ map()         │ nuevo array │ Transforma cada elemento                              │
│ filter()      │ nuevo array │ Elementos que cumplen condición                       │
│ reduce()      │ valor único │ Reduce a un único valor (izq → der)                   │
│ reduceRight() │ valor único │ Reduce a un único valor (der → izq)                   │
│ some()        │ boolean     │ ¿Alguno cumple la condición?                          │
│ every()       │ boolean     │ ¿Todos cumplen la condición?                          │
│ entries()     │ iterador    │ Iterador de pares [índice, valor]                     │
│ keys()        │ iterador    │ Iterador de índices                                   │
│ values()      │ iterador    │ Iterador de valores                                   │
└───────────────┴─────────────┴───────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         MÉTODOS DE CONVERSIÓN                                       │
├───────────────┬─────────────┬───────────────────────────────────────────────────────┤
│ Método        │ Devuelve    │ Descripción                                           │
├───────────────┼─────────────┼───────────────────────────────────────────────────────┤
│ join()        │ string      │ Une elementos con separador                           │
│ toString()    │ string      │ Convierte a string (sep: coma)                        │
│ toLocaleString│ string      │ Convierte con formato regional                        │
└───────────────┴─────────────┴───────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          MÉTODOS ESTÁTICOS (NO DE INSTANCIA)                        │
├───────────────────────┬─────────────┬───────────────────────────────────────────────┤
│ Método                │ Devuelve    │ Descripción                                   │
├───────────────────────┼─────────────┼───────────────────────────────────────────────┤
│ Array.isArray()       │ boolean     │ Verifica si es array                          │
│ Array.from()          │ nuevo array │ Crea array desde iterable                     │
│ Array.of()            │ nuevo array │ Crea array con los argumentos dados           │
│ Object.groupBy()      │ objeto      │ Agrupa elementos por clave (ES2024)           │
│ Map.groupBy()         │ Map         │ Agrupa en Map (ES2024)                        │
└───────────────────────┴─────────────┴───────────────────────────────────────────────┘
*/

//--------------------------------------------------------------------------------------
// 🎯 GUÍA RÁPIDA DE DECISIÓN
//--------------------------------------------------------------------------------------

// ¿Necesitas BUSCAR?
// - ¿Existe el valor? → includes()
// - ¿Dónde está? → indexOf() / lastIndexOf()
// - ¿Con condición? → find() / findIndex()

// ¿Necesitas FILTRAR/TRANSFORMAR?
// - Filtrar → filter()
// - Transformar → map()
// - Ambos → filter().map() o flatMap()

// ¿Necesitas AGREGAR/ELIMINAR?
// - Al final → push() / pop()
// - Al inicio → unshift() / shift()
// - En cualquier posición → splice()

// ¿Necesitas REDUCIR?
// - Un solo valor → reduce()
// - Suma/contador → reduce()
// - Objeto/Map → reduce() o Object.groupBy()

// ¿Necesitas VALIDAR?
// - ¿Alguno cumple? → some()
// - ¿Todos cumplen? → every()

// ¿Necesitas ORDENAR?
// - Modificando original → sort()
// - Sin modificar → toSorted()

// ¿Necesitas COPIAR?
// - Copia completa → [...array] o slice()
// - Copia parcial → slice(inicio, fin)
